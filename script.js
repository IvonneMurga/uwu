// Obtener elementos
const showGatitoBtn = document.getElementById('showGatitoBtn');
const gatitoContainer = document.getElementById('gatitoContainer');

// Mostrar el gatito cuando se presiona el botón
showGatitoBtn.addEventListener('click', () => {
    gatitoContainer.style.display = 'block'; // Mostrar el gatito
});
